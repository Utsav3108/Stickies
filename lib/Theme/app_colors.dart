import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Background
  static const Color background = Colors.black;

  // Sticky text
  static const Color textPrimary = Colors.white;
  static const Color textHint = Colors.white54;

  // Chips
  static const Color textChip = Color(0xFFD8DD56);
  static const Color photoChip = Color(0xFF7C74F5);
  static const Color videoChip = Color(0xFFF5AA74);

  // Save button
  static const Color saveButton = Color(0xFFA4F291);

  // Opacity
  static const double activeOpacity = 1.0;
  static const double inactiveOpacity = 0.5;
}
